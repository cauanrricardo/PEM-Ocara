{
  "name": "PEMOCARA",
  "version": "1.0.0",
  "description": "PEM OCARA",
  "main": "dist/main.js",
  "scripts": {
    "build:main": "tsc --project configs/tsconfig.json",
    "build:form": "tsc --project configs/tsconfig.form.json", 
    "build:users": "tsc --project configs/tsconfig.users.json",
    "build:listarAssistida": "tsc --project configs/tsconfig.listarAsistida.json",
    "build:login": "tsc --project configs/tsconfig.login.json",
  
    "build": "npm run build:main && npm run build:form && npm run build:users && npm run build:login",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron ."
  },
  "keywords": [
    "electron",
    "typescript",
    "mvc",
    "secure"
  ],
  "author": "Joao-Vitor-Rd",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.19.23",
    "electron": "^28.0.0",
    "typescript": "^5.3.0"
  }
}
